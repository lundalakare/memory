# Migration `20200621165032-templates-rename`

This migration has been generated by elliotsoomro at 6/21/2020, 4:50:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Template" (
"back" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"front" text  NOT NULL ,"id" text  NOT NULL ,"name" text  NOT NULL ,"noteTypeId" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."User" ALTER COLUMN "role" SET DEFAULT E'USER';

ALTER TABLE "public"."CardType" DROP CONSTRAINT IF EXiSTS "CardType_noteTypeId_fkey";

ALTER TABLE "public"."Template" ADD FOREIGN KEY ("noteTypeId")REFERENCES "public"."NoteType"("id") ON DELETE CASCADE  ON UPDATE CASCADE

DROP TABLE "public"."CardType";
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200613124503-prisma-refactor..20200621165032-templates-rename
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -30,17 +30,17 @@
 model NoteType {
   id          String      @id @default(cuid()) 
   name        String
   fields      String[]
-  cardTypes   CardType[]
+  templates   Template[]
   notes       Note[]
   user        User        @relation(fields: [userId], references: [id])
   userId      String
   createdAt   DateTime    @default(now())
   updatedAt   DateTime    @updatedAt()
 }
-model CardType {
+model Template {
   id          String   @id @default(cuid())
   name        String
   front       String
   back        String
```


