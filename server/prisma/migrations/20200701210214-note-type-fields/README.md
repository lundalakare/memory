# Migration `20200701210214-note-type-fields`

This migration has been generated by elliotsoomro at 7/1/2020, 9:02:14 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Field" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" text  NOT NULL ,"index" integer  NOT NULL ,"name" text  NOT NULL ,"noteTypeId" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

ALTER TABLE "public"."NoteType" DROP COLUMN "fields";

CREATE UNIQUE INDEX "Field.name_noteTypeId" ON "public"."Field"("name","noteTypeId")

CREATE UNIQUE INDEX "Field.index_noteTypeId" ON "public"."Field"("index","noteTypeId")

ALTER TABLE "public"."Field" ADD FOREIGN KEY ("noteTypeId")REFERENCES "public"."NoteType"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200623132730-initial-migration..20200701210214-note-type-fields
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -22,17 +22,30 @@
 model NoteType {
   id          String      @id @default(cuid()) 
   name        String
-  fields      String[]
+  fields      Field[]
   templates   Template[]
   notes       Note[]
   user        User        @relation(fields: [userId], references: [id])
   userId      String
   createdAt   DateTime    @default(now())
   updatedAt   DateTime    @updatedAt()
 }
+model Field {
+  id          String    @id @default(cuid())
+  name        String
+  index       Int
+  noteType    NoteType  @relation(fields: [noteTypeId], references: [id])
+  noteTypeId  String
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime @updatedAt()
+
+  @@unique([name, noteTypeId])
+  @@unique([index, noteTypeId])
+}
+
 model Template {
   id          String   @id @default(cuid())
   name        String
   front       String
```


